# 🤖 بوت تلغرام لتحميل الفيديوهات

بوت تلغرام متكامل ومجاني بالكامل، مبني باستخدام لغة Python، يقوم بتحميل الفيديوهات والصوتيات من أشهر المنصات العالمية مباشرة داخل تلغرام. يتميز البوت بدعمه للغات متعددة، سهولة الاستخدام، وقابليته للتطوير.

![صورة توضيحية للبوت](https://i.imgur.com/example.png) <!-- رابط صورة وهمي، يمكن استبداله بلقطة شاشة حقيقية -->

---

## 🎯 الميزات الرئيسية

- **دعم واسع للمنصات**: تحميل من YouTube, TikTok, Facebook, Instagram, Twitter/X, Dailymotion, Vimeo, SoundCloud وغيرها.
- **تحميل ستوريات ومنشورات إنستغرام**: دعم كامل لتحميل القصص (Stories) والمنشورات المتعددة (Carousel) والريلز (Reels).
- **التعرف التلقائي على الروابط**: ما عليك سوى إرسال الرابط ليبدأ البوت عمله.
- **خيارات جودة متعددة**: اختر جودة الفيديو المناسبة لك من قائمة منظمة (144p, 360p, 720p, 1080p, وأفضل جودة متاحة).
- **تحميل الصوت فقط**: إمكانية استخلاص الصوت من الفيديوهات وحفظه بصيغة MP3 عالية الجودة.
- **عرض معلومات الفيديو**: يعرض البوت تفاصيل الفيديو (العنوان، المدة، المشاهدات) قبل التحميل.
- **تقدير حجم الملف**: عرض الحجم التقريبي للملف لكل جودة متاحة لمساعدتك في الاختيار.
- **دعم متعدد اللغات**: واجهة كاملة باللغتين العربية والإنجليزية مع سهولة التبديل بينهما.
- **معالجة متقدمة للأخطاء**: يتعامل البوت بذكاء مع الروابط غير الصالحة، الفيديوهات المحذوفة، أو المشاكل أثناء التحميل دون أن يتوقف عن العمل.
- **نظام مكافحة السبام**: يمنع الاستخدام المفرط عن طريق تحديد عدد الطلبات لكل مستخدم في فترة زمنية معينة.
- **سهولة الإعداد والتشغيل**: يتضمن سكريبت تثبيت تلقائي يجعل عملية الإعداد بسيطة للغاية.
- **جاهز للنشر (Deployment-Ready)**: يتضمن ملفات `Dockerfile`, `docker-compose.yml`, `railway.toml`, و `render.yaml` لسهولة رفعه على أشهر منصات الاستضافة السحابية.
- **استخدام ملف الكوكيز (Cookies)**: تم إضافة دعم لملفات الكوكيز لتجاوز قيود إنستغرام وتويتر عند الضرورة.

---

## ⚙️ المتطلبات الأساسية

قبل البدء، تأكد من أن نظامك يحتوي على:

- **Python 3.8+**
- **FFmpeg**: أداة أساسية لمعالجة ودمج ملفات الفيديو والصوت. (سيحاول سكريبت الإعداد تثبيته تلقائيًا).
- **Git**: لنسخ المشروع من المستودع.

---

## 🚀 خطوات الإعداد والتشغيل (محليًا أو على VPS)

اتبع هذه الخطوات بالتفصيل لإعداد وتشغيل البوت على جهازك المحلي أو خادم VPS.

### الخطوة 1: إنشاء البوت والحصول على التوكن

1.  افتح تطبيق تلغرام وابحث عن `@BotFather`.
2.  ابدأ محادثة معه وأرسل الأمر `/newbot`.
3.  اختر اسمًا للبوت (مثلاً: `Video Downloader`).
4.  اختر معرفًا فريدًا للبوت (يجب أن ينتهي بـ `bot`، مثلاً: `MyVideoDownloaderBot`).
5.  بعد نجاح الإنشاء، سيمنحك `BotFather` **توكن (Token)** خاصًا ببوتك. احتفظ به، فهو مفتاح الوصول إلى بوتك.

    > **⚠️ هام جداً:** لا تشارك هذا التوكن مع أي شخص. من يملكه يمكنه التحكم ببوتك بالكامل.

### الخطوة 2: نسخ المشروع إلى جهازك

افتح الطرفية (Terminal) ونفّذ الأمر التالي لنسخ المشروع:

```bash
git clone https://github.com/manus-team/telegram-video-downloader-bot.git
cd telegram-video-downloader-bot
```

### الخطوة 3: تشغيل سكريبت الإعداد التلقائي

لقد تم تجهيز سكريبت يقوم بكل شيء من أجلك: تثبيت المتطلبات، إعداد البيئة، وطلب التوكن.

1.  أعطِ السكريبت صلاحيات التنفيذ:
    ```bash
    chmod +x setup.sh
    ```

2.  شغّل السكريبت:
    ```bash
    ./setup.sh
    ```

3.  سيقوم السكريبت بالآتي:
    - التحقق من وجود `python` و `pip`.
    - محاولة تثبيت `ffmpeg`.
    - إنشاء بيئة Python افتراضية (`venv`) لتجنب تعارض المكتبات.
    - تثبيت جميع مكتبات Python المطلوبة من ملف `requirements.txt`.
    - إنشاء ملف `.env` من المثال `env.example`.
    - **سيطلب منك إدخال توكن البوت** الذي حصلت عليه من `BotFather`.

### الخطوة 4: تشغيل البوت

بعد اكتمال الإعداد بنجاح، يمكنك تشغيل البوت بطريقتين:

-   **للتشغيل العادي (للتجربة والتطوير):**
    ```bash
    ./start_bot.sh
    ```
    سيظل البوت يعمل طالما أن نافذة الطرفية مفتوحة.

-   **للتشغيل في الخلفية (24/7 على خادم VPS):**
    استخدم `nohup` لإبقاء البوت يعمل حتى بعد إغلاق جلسة SSH.
    ```bash
    nohup ./start_bot.sh > bot.log 2>&1 &
    ```
    -   لمتابعة سجلات البوت (Logs):
        ```bash
        tail -f bot.log
        ```
    -   لإيقاف البوت، ستحتاج إلى العثور على معرّف العملية (PID) وقتلها:
        ```bash
        ps aux | grep main.py
        kill <PID>
        ```

---

## ☁️ طريقة رفعه وتشغيله 24/7 مجانًا (باستخدام Railway)

البوت مجاني بالكامل ويمكن تشغيله على مدار الساعة باستخدام منصات الاستضافة السحابية التي توفر خططًا مجانية. **Railway** هي الخيار الموصى به لسهولتها وكرم خطتها المجانية.

### الخطوة 1: ارفع المشروع إلى حسابك على GitHub

1.  **أنشئ مستودعًا جديدًا (New Repository)** على GitHub. يمكنك تسميته `telegram-video-downloader-bot`.
2.  **ارفع ملفات المشروع** التي قمت بتنزيلها إلى هذا المستودع. تأكد من رفع جميع الملفات والمجلدات (باستثناء مجلد `venv` وملف `bot.log` إذا قمت بتشغيل البوت محليًا).
    ```bash
    git init
    git add .
    git commit -m "Initial commit: Telegram Video Downloader Bot"
    git branch -M main
    git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
    git push -u origin main
    ```
    > **ملاحظة:** تأكد من استبدال `YOUR_USERNAME` و `YOUR_REPO_NAME` بمعلوماتك.

### الخطوة 2: أنشئ حسابًا على Railway

1.  اذهب إلى [Railway.app](https://railway.app/).
2.  سجّل الدخول باستخدام حسابك في GitHub. سيطلب منك Railway صلاحية الوصول إلى مستودعاتك، وافق على ذلك.

### الخطوة 3: أنشئ مشروعًا جديدًا على Railway

1.  من لوحة تحكم Railway، اضغط على زر `New Project`.
2.  اختر `Deploy from GitHub Repo`.
3.  ابحث عن المستودع الذي رفعته في الخطوة 1 (مثلاً: `telegram-video-downloader-bot`) واختره.
4.  سيقوم Railway تلقائيًا باكتشاف ملف `Dockerfile` و `railway.toml` وبدء عملية البناء (Build).

### الخطوة 4: أضف متغيرات البيئة (BOT_TOKEN)

1.  بعد إنشاء المشروع، اذهب إلى تبويب `Variables` في لوحة تحكم مشروعك على Railway.
2.  اضغط على `New Variable` وأضف المتغيرات التالية:
    -   **Variable Name**: `BOT_TOKEN`
        **Value**: `YOUR_BOT_TOKEN_HERE` (استبدله بالتوكن الحقيقي الذي حصلت عليه من BotFather).
    -   (اختياري) **Variable Name**: `COOKIE_FILE`
        **Value**: `./config/cookies.txt` (إذا كنت تستخدم ملف الكوكيز لإنستغرام).

    > **هام:** Railway سيقوم بإعادة نشر البوت تلقائيًا بعد إضافة المتغيرات.

### الخطوة 5: (اختياري) إضافة ملف الكوكيز لإنستغرام

إذا كنت ترغب في تحميل القصص والمنشورات الخاصة من إنستغرام، قد تحتاج إلى توفير ملف كوكيز:

1.  **استخرج ملف الكوكيز:** استخدم إضافة متصفح مثل **"Get cookies.txt LOCALLY"** (متوفرة لـ Chrome و Firefox) لتصدير الكوكيز الخاصة بإنستغرام إلى ملف باسم `cookies.txt`.
2.  **أنشئ مجلد `config`** داخل مجلد المشروع الرئيسي (إذا لم يكن موجودًا) وضع ملف `cookies.txt` بداخله.
3.  **ارفع ملف `cookies.txt`** إلى مستودع GitHub الخاص بك داخل مجلد `config`.
4.  **أضف متغير البيئة `COOKIE_FILE`** في Railway كما هو موضح في الخطوة 4.

### الخطوة 6: تحقق من حالة البوت

1.  اذهب إلى تبويب `Deployments` في مشروعك على Railway.
2.  ستشاهد سجلات البناء والنشر. إذا كان كل شيء على ما يرام، ستظهر حالة `Deployed`.
3.  يمكنك النقر على `View Logs` لمتابعة عمل البوت والتأكد من عدم وجود أخطاء.

**تهانينا!** بوتك يعمل الآن على مدار الساعة مجانًا على Railway.

---

## 📂 هيكل المشروع

```
/telegram-downloader-bot
├── config/          # ملفات الإعدادات الرئيسية (التوكن، حدود الحجم، إلخ)
├── handlers/        # معالجات أوامر ورسائل تلغرام
├── locales/         # ملفات الترجمة والرسائل متعددة اللغات
├── utils/           # الوحدات المساعدة (التحميل، التحقق من الروابط، إلخ)
├── .env.example     # ملف مثال لمتغيرات البيئة
├── .gitignore       # الملفات والمجلدات التي يجب تجاهلها
├── Dockerfile       # لإنشاء حاوية Docker للبوت
├── docker-compose.yml # لتشغيل البوت محليًا باستخدام Docker
├── main.py          # نقطة الدخول الرئيسية للبوت
├── README.md        # ملف التوثيق هذا
├── railway.toml     # ملف إعدادات النشر على Railway
├── render.yaml      # ملف إعدادات النشر على Render
├── requirements.txt # قائمة مكتبات Python المطلوبة
├── setup.sh         # سكريبت الإعداد التلقائي
└── start_bot.sh     # سكريبت بسيط لتشغيل البوت
```

---

## 🧠 اقتراحات تحسين مستقبلية

-   **قوائم الانتظار (Queue System)**: استخدام نظام مثل `Celery` مع `Redis` لمعالجة طلبات التحميل بشكل متزامن، مما يمنع تجميد البوت عند التعامل مع ملفات كبيرة.
-   **قاعدة بيانات للمستخدمين**: استخدام قاعدة بيانات (مثل `SQLite` أو `PostgreSQL`) لتخزين تفضيلات المستخدمين وإحصائيات الاستخدام بشكل دائم.
-   **دعم قوائم التشغيل (Playlists)**: إضافة ميزة تحميل جميع الفيديوهات من قائمة تشغيل على يوتيوب.
-   **لوحة تحكم للمشرف**: إنشاء أوامر خاصة بالمسؤولين لمراقبة أداء البوت، حظر المستخدمين، أو بث رسائل للجميع.
-   **تقسيم الملفات الكبيرة**: إضافة القدرة على تقسيم الملفات التي تتجاوز حد 50 ميجابايت إلى أجزاء أصغر وإرسالها.
-   **دعم Webhooks**: التبديل من `Polling` إلى `Webhooks` لتحسين الأداء وتقليل استهلاك الموارد على الخادم.

---

## 🤝 المساهمة

المساهمات مرحب بها دائمًا! إذا كانت لديك فكرة لتحسين البوت أو إصلاح لمشكلة، لا تتردد في فتح `Pull Request`.

## 📜 الترخيص

هذا المشروع مرخص تحت رخصة MIT. انظر ملف `LICENSE` لمزيد من التفاصيل.
